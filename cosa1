import numpy as np
import matplotlib.pyplot as plt

·evenlysampled time at 200ms intervals
t = no.arange(0., 5., 0.2)

·red dasehes, blue squares and gree trianles
plt.plot (t, t, 'r--', t, t**2, 'bs't, t**3, 'g^')
plt.show()
